name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Récupérer le code
    - name: Checkout code
      uses: actions/checkout@v4

    # 2️⃣ Télécharger & installer Railway CLI (nouvelle version)
    - name: Install Railway CLI
      run: |
        curl -fsSL https://railway.app/install.sh | sh
        echo "$HOME/.railway/bin" >> $GITHUB_PATH

    # 3️⃣ Vérifier la version CLI
    - name: Check Railway CLI version
      run: railway --version

    # 4️⃣ Déployer avec Railway
    - name: Deploy to Railway
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: |
        echo "🚀 Déploiement en cours avec nouvelle CLI..."
        railway up --service InterfaceOradia --detach
