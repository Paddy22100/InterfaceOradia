name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Récupérer le code
    - name: Checkout code
      uses: actions/checkout@v4

    # 2️⃣ Installer la CLI Railway
    - name: Install Railway CLI
      run: npm install -g railway

    # 3️⃣ Définir la variable PORT pour FastAPI
    - name: Set PORT environment variable
      run: echo "PORT=8000" >> $GITHUB_ENV

    # 4️⃣ Ajouter un Procfile si absent
    - name: Ensure Procfile exists for FastAPI
      run: |
        if [ ! -f Procfile ]; then
          echo "web: uvicorn server:app --host 0.0.0.0 --port \${PORT}" > Procfile
          echo "✅ Procfile créé."
        else
          echo "ℹ️ Procfile déjà présent."
        fi

    # 5️⃣ Lier le projet Railway
    - name: Link Railway project
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: railway link f1f4b864-dc6a-48a6-b871-0eb83d846bcd

    # 6️⃣ Déployer : railway up puis fallback sur railway deploy
    - name: Deploy to Railway (hybrid up/deploy)
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        PORT: ${{ env.PORT }}
      run: |
        echo "🚀 Tentative de déploiement rapide avec railway up..."
        if railway up --service InterfaceOradia --detach; then
          echo "✅ Déploiement rapide réussi !"
        else
          echo "⚠️ railway up a échoué. Tentative avec railway deploy..."
          railway deploy --service InterfaceOradia
        fi
